{
  "description": "Concept structure for Firebase Security Rules, demonstrating user-level authorization and role-based access control (RBAC) to protect sensitive administrative data and private user records.",
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "admin_config": {
      ".read": "auth != null && auth.token.role === 'admin'",
      ".write": "auth != null && auth.token.role === 'admin'"
    },

    "public_posts": {
      ".read": true,
      ".write": "auth != null"
    }
  }
}
